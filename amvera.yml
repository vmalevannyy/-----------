meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: npm run build
run:
  command: npm run start -- -p 3000
  persistenceMount: /app/public
  containerPort: "3000"
