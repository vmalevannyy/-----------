meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: npm run build
run:
  command: node .next/standalone/server.js
  persistenceMount: /app/public
  containerPort: "3000"
